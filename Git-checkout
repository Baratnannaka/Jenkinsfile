pipeline{
    agent any
    stages{
        stage("git checokeout"){
            steps{
               git 'https://github.com/Baratnannaka/maven-practice.git' 
            }
        }
        stage("mvn build"){
            steps{
                sh "mvn clean package"
            }
        }
    }
}
